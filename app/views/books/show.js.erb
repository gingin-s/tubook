document.getElementById("notes").innerHTML = "<%= escape_javascript(render(partial: 'notes/note', collection: @notes)) %>";
document.getElementById("add-note-wrapper").innerHTML = "<%= escape_javascript(render(partial: 'books/note_form', locals: { book: @book, note: @note })) %>";
var notes = <%= @notes.to_json.html_safe %>;
afterCreateNote(notes);